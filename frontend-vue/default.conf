server {
  listen 80;
  server_name localhost;

  # root path where Vue builds are served
  root /usr/share/nginx/html;
  index index.html;

  # handle requests for static assets
  location / {
    try_files $uri $uri/ /index.html;
  }
}
